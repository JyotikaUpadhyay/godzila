{
  "name": "bluefly",
  "private": true,
  "scripts": {
    "install:all": "npm i --prefix backend/server && npm i --prefix frontend/client",
    "dev": "npm run dev:backend & npm run dev:frontend",
    "dev:frontend": "npm run dev --prefix frontend/client",
    "dev:backend": "npm run dev --prefix backend/server"
  }
}
